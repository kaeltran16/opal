import 'package:flutter/widgets.dart';

import '../shell/loop_shell.dart';

/// Nutrition landing — temporary stub so the router compiles. Replaced by the
/// real screen in a later task.
class NutritionScreen extends StatelessWidget {
  const NutritionScreen({super.key});

  @override
  Widget build(BuildContext context) => const PlaceholderScreen(label: 'Nutrition');
}
