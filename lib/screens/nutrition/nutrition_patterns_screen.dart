import 'package:flutter/widgets.dart';

import '../shell/loop_shell.dart';

/// Connections / patterns — temporary stub so the router compiles. Replaced by
/// the real screen in a later task.
class NutritionPatternsScreen extends StatelessWidget {
  const NutritionPatternsScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const PlaceholderScreen(label: 'Connections');
}
