# App-wide audit — 2026-06-12

Eight parallel read-only audits covered every screen, controller, service, widget, and the data layer.
`flutter analyze` baseline: clean (one info lint in `test/controllers/widget_sync_controller_test.dart:61`).

Status legend:
- **fixed** — fixed in this pass (verified: `flutter analyze` clean, 148/148 tests pass)
- **deferred** — needs a product/design decision or is a larger refactor; not safe to do unilaterally
- **refuted** — investigated, not a real issue

---

## Core / shell / theme / widgets

### Errors
- `lib/theme/app_colors.dart:139` — **fixed** — `copyWith(brightness:)` is a no-op: both branches of the final return yield `this`, and the accent path hardcodes the old brightness. Brightness-only changes never recolor.
- `lib/widgets/activity_rings.dart:180` — **fixed** — `shouldRepaint` compares lists by identity, so the guard never short-circuits; compare element-wise.
- `lib/main.dart:28` — **fixed** — timezone init has no error handling; a bad device IANA zone crashes startup before `runApp`. Fall back to UTC.

### Inconsistencies
- `lib/widgets/loop_tab_bar.dart:112` / `lib/screens/shell/loop_shell.dart:39` — **fixed** — FAB `semanticLabel` says "Quick actions" and comments still say Quick Actions, but the FAB opens the Pal composer.
- `lib/screens/shell/loop_shell.dart:19` — **fixed** — `_tabIds` uses `'profile'` while the route is `you`; tab id, route name, and branch order rely on positional alignment. Align naming / derive from one source.
- `lib/router.dart:389` — deferred — Pal composer barrier color is a hardcoded scrim instead of a theme token (part of the white/scrim token decision below).
- Raw white/transparent/black literals across shared widgets and ~15 screens (`Color(0xFFFFFFFF)` duplicated, incl. seven per-file `_white` constants in workout) — **deferred** — fixing properly means adding `AppColors` tokens (`onAccent`, scrim) and a broad mechanical sweep; worth doing as its own pass.

### Improvements
- `lib/widgets/nav_bar.dart:9` — refuted — `LargeTitleNavBar` is not dead: referenced by routine_editor_screen, rituals_builder_screen, and email_nav. Left in place.
- `lib/widgets/loop_tab_bar.dart:36` — deferred — full-width `BackdropFilter` with no `RepaintBoundary`; measure before optimizing.
- `lib/app.dart:64` — refuted — deep-link dedupe window is the intended duplicate-suppression behavior.

## Today / entry / detail / quick actions

### Errors
- `lib/screens/entry/new_entry_sheet.dart:256` — **fixed** — `_applyPick` clears the note instead of writing `p.detail`, so quick-pick detail ("Verve Coffee") is silently dropped.
- `lib/controllers/today_controller.dart:153` — **fixed** — week mode nests a one-shot `.first` inside the today stream, so edits to older same-week days never re-emit; drive week mode from `watchEntriesInRange` directly.

### Inconsistencies
- `lib/screens/today/today_screen.dart:515,519` — **fixed** — money rows use `goNamed` while workout rows use `pushNamed` for sibling detail destinations; standardize on push for detail screens (verify against router).
- `lib/screens/today/today_screen.dart:432,173` — deferred — day-progress bar (`0.88`) and `'DAY · 21:30'` labels are hardcoded placeholders; needs real data design.

### Improvements
- `lib/controllers/today_controller.dart:22` — **fixed** — `TimelineModeController.toggle()` is dead code.
- `lib/screens/detail/detail_screen.dart:37` — **fixed** — raw exception interpolated into user-facing copy; show generic message, log the error.
- `lib/screens/today/today_screen.dart:117` — deferred — Search nav button has no `onTap` (see "dead controls" decision below).
- `lib/screens/entry/new_entry_sheet.dart:289` — deferred — snackbar messenger target inside dismissing sheet; low confidence, verify behavior first.

## Workout

### Errors
- `lib/controllers/workout_session.dart:128` — **fixed** — `addSet` derives set ids from `exerciseSets.length`, which can collide after prior adds; use max-suffix+1 or a counter. Also guard the unprotected `.last` at :115.
- `lib/screens/workout/active_session_screen.dart:192` — **fixed** — header elapsed time only updates on rebuilds, so the workout clock visually freezes; drive it with a periodic timer.
- `lib/screens/workout/routine_editor_screen.dart:190` — **fixed** — `_NameField` never syncs its controller in `didUpdateWidget`.
- `lib/screens/workout/routine_editor_screen.dart:288` — **fixed** — `_RestSection` displays the nearest preset as selected while persisting a different off-preset value; displayed and stored state disagree.
- `lib/screens/workout/active_session_screen.dart:126` — deferred — verify keyed provider disposal after `pushReplacementNamed` on finish; medium confidence, needs runtime verification.

### Inconsistencies
- `lib/screens/workout/active_session_screen.dart:130` — **fixed** — raw `'postWorkout'` string instead of `AppRoute.postWorkout.name`.
- `lib/screens/workout/workout_detail_screen.dart:215` — **fixed** — delete button on a workout session says "Delete routine".
- `lib/screens/workout/active_session_screen.dart:766,879,1026` — **fixed** — bare `GestureDetector`s for complete-set/add-set/back where the app standard is `PressScale`.
- Weight formatting (`_trimDouble` etc.) reimplemented 4× across the feature — **fixed** — extracted `formatWeight` in `lib/util/format.dart`, used at all four sites. A fifth site (`post_workout_screen.dart` `_SetBar` ~:681) was found during fixing and left for a follow-up.
- `active_session_screen.dart:108` — deferred — raw Material `AlertDialog` for finish confirmation, only unstyled modal in the feature; styling decision.
- Set rows hand-roll a check control instead of shared `CheckButton` (also rituals `_StepRow`) — **deferred** — adoption changes visuals/haptics in two features; do as one deliberate pass.
- Scaffold vs `ColoredBox` page-background split across the six screens — deferred — pick one pattern; cosmetic/structural.

### Improvements
- `lib/screens/workout/routine_editor_screen.dart:459` — **fixed** — hint promises "swipe left to remove" but no `Dismissible` exists; drop the phrase (xmark button already removes).
- `lib/controllers/routine_generator_controller.dart:84` — **fixed** — `generate` swallows the exception with no logging.
- `lib/screens/workout/routine_generator_screen.dart:70` — **fixed** — `_save()` has no error handling unlike post-workout's save; mirror the snackbar pattern.
- `lib/screens/workout/active_session_screen.dart:79` — **fixed** — rest-banner +30s/Skip lack button semantics.
- `active_session_screen.dart:584` — deferred — decorative no-op ellipsis menu (see "dead controls").

## Rituals / reflect

### Errors
- `ReorderableListView(onReorderItem:)` "doesn't compile" / controller index semantics — **refuted** — `onReorderItem` is the current Flutter API (post-removal `newIndex`, replaces obsolete `onReorder`); verified in the installed SDK. Controller logic is correct.
- `lib/controllers/profile_controller.dart:134` + `streak_celebration_screen.dart` — **deferred** — celebrated "workout streak" is actually computed from seeded ritual streaks that are never incremented; the streak's real data source is a product decision.
- `lib/screens/rituals/routine_player_screen.dart:62` — deferred — completion streak "+1" is cosmetic only (never persisted); same product decision as above.

### Inconsistencies
- `lib/screens/reflect/evening_close_out_screen.dart:268` — **fixed** — re-implements first-incomplete-step flattening the controller already exposes (`firstIncompleteStep`); leftover of the retired flat ritual model.
- `lib/screens/rituals/rituals_builder_screen.dart:248` — **fixed** — `_openEditor` pops with the outer context across an async callback without a mounted guard.
- Three private dashed-border painters (`rituals_builder`, `rituals_screen`, `evening_close_out`) — deferred — promote one to `lib/widgets` in a dedup pass.
- `streak_celebration_screen.dart:197` — deferred — Share CTA has empty `onTap` (see "dead controls").

### Improvements
- `lib/screens/rituals/routine_player_screen.dart:46` — **fixed** — error branch discards the actual error; log it like sibling screens.
- `lib/screens/rituals/rituals_screen.dart:213` — **fixed** — `~${left * 5} min` magic number; name the constant.

## Pal

### Errors
- `lib/controllers/pal_action_executor.dart:42` — **fixed** — `applyPalActions` has no rollback: a mid-loop throw leaves earlier mutations applied, unreported, and not undoable. Roll back already-applied actions on failure.
- `lib/services/pal/http_pal_service.dart:164,235,240,244` — **fixed** — non-null `as String` casts on proxy JSON throw raw `TypeError`s instead of `PalException`; decode defensively and normalize failures to `PalException` so the composer's offline path is reached consistently.
- `lib/controllers/pal_composer_controller.dart:155` — **deferred** — composer auto-applies mutations but discards `AppliedActions` and renders no undo, diverging from Ask-Pal; needs a UX decision (add undo affordance vs stop auto-applying).
- `lib/controllers/ask_pal_controller.dart:61` — **fixed** — post-await state writes lack a disposal guard.

### Inconsistencies
- `lib/screens/pal/ask_pal_screen.dart:549,436` — **fixed** — send button and suggestion chips are bare `GestureDetector`s while the composer uses `PressScale` for the identical controls.
- `ask_pal_screen.dart:134` vs `pal_inbox_screen.dart:72` — **fixed** — back nav split between `context.pop()` and `Navigator.maybePop()`; standardize.
- Pal gradient avatar duplicated between composer and inbox — deferred — dedup pass.
- Money formatting differs between composer confirmations and mock `_ack` — **fixed** — share one formatting helper.

### Improvements
- `lib/screens/pal/pal_inbox_screen.dart:219` — **fixed** — footer announces as a button to screen readers but has no `onTap`; remove the button semantics until wired.
- `lib/services/pal/pal_context_builder.dart:9` — **fixed** — money formatted with `toStringAsFixed(0)` drops cents from the model context.
- `ask_pal_screen.dart:138` — deferred — "More options" dead control (see "dead controls").
- Token handling — verified clean: no committed secrets; tokens live in secure storage only.

## Email

### Errors
- `lib/controllers/email_sync_controller.dart:195` — **fixed** — `syncNow()` has no try/catch; a service throw leaves the dashboard stuck and the future rejects unhandled. Same for `testConnection` at :55 (stuck on `testing` forever).
- `lib/screens/email/email_dashboard_screen.dart:142` — **fixed** — Sync-now tap fires an unawaited, unhandled future.
- `lib/screens/email/email_setup_screen.dart:62` — **fixed** — `_save()` navigates to the dashboard even if `connect()` throws.
- `lib/screens/email/email_dashboard_screen.dart:54` — **fixed** — hero/subtitle hardcode "Gmail · connected" regardless of `dash.isConnected`.
- `lib/services/email/mock_email_sync_service.dart:46` — deferred — mock never emits `SyncStatus.error`, so the error UI is unreachable in dev; adding a failure path is a dev-tooling decision.

### Inconsistencies
- Gmail glyph painter (~90 lines) triplicated across intro/setup/dashboard — **fixed** — extract one shared widget.
- `email_setup_screen.dart:48` — **fixed** — screen hardcodes `appPasswordRef`; let the service own it in both impls.
- "ExpensePal" vs app branding in intro/setup copy — **deferred** — confirm intended product name.
- `email_nav.dart` bespoke small-title nav vs shared nav widgets — deferred — structural.

### Improvements
- `lib/services/email/real_email_sync_service.dart:101` — **fixed** — `json['items'] as List` throws on malformed response; default to empty.
- Both sync services leak their broadcast `StreamController` — **fixed** — put `dispose()` on the interface and wire `ref.onDispose`.
- `email_nav.dart:84` — **fixed** — `AppPasswordFormatter` force-lowercases input; only strip/group spaces.
- `email_dashboard_screen.dart:259` — **fixed** — disconnect throw is unhandled.
- Credentials verified clean: app password only in secure storage, sent per-request, never persisted to the model.

## Move / money / review

### Errors
- `lib/controllers/move_controller.dart:188` — **fixed** — `weekWorkouts` has no upper date bound, so future-dated workouts pollute "this week" stats.
- `lib/controllers/move_controller.dart:163` — refuted — `WorkoutRepository.watchWorkouts()` explicitly orders by `startedAt` descending, so `take(3)` is correct as written.
- `lib/screens/move/weekly_plan_screen.dart:544` — **fixed** — schedule rows render a chevron but have no `onTap`; drop the chevron until the row has an action.
- `lib/widgets/budget_sheet.dart:50` — **fixed** — weekly→daily rescale duplicated between display and save; single helper.
- `lib/controllers/weekly_review_controller.dart:171` — deferred — goals fold may use defaults pre-load; low confidence, verify reactivity before changing.

### Inconsistencies
- `weekly_plan_screen.dart:62` / `weekly_review_screen.dart:45` — refuted — measured both layouts: weekly (16px outer + 12px internal) and monthly (`left: 16` + 12px internal) both land the back glyph at 28px. Already aligned; no change.
- Weekly Plan and Weekly Review hand-roll nav/list structure instead of `LargeTitleScrollView`/`InsetSection` like their siblings — **deferred** — structural refactor, do deliberately with visual review.
- `weekly_review_screen.dart:55` — **fixed** — share icon missing `semanticLabel`.

### Improvements
- `weekly_plan_screen.dart:357` — deferred — "Swap" CTA duplicates "Start workout" (no swap flow exists); product decision.
- `move_screen.dart:559,601` — deferred — inert "History & trends" row and misleading "See all"; product decision.
- `lib/controllers/monthly_review_controller.dart:193` — **fixed** — streak row caption says "Workouts, ongoing" but the data source is ritual routines.
- `lib/controllers/insights_controller.dart:15` — **fixed** — `insightIcon` has no rituals case; falls through to sparkles.

## Settings / profile / onboarding

### Errors
- `lib/screens/settings/notifications_screen.dart:51` — **deferred** — reminder toggles persist a bool but never schedule/cancel notifications; the toggle has no delivery effect. Needs notification-scheduling design.
- `lib/controllers/profile_controller.dart:162` — **fixed** — `profileStats` one-shots `ritualRepo.getAll()` inside the entries stream, so ritual changes don't refresh stats; combine the streams.

### Inconsistencies
- `lib/screens/settings/appearance_screen.dart:27` — **fixed** — only settings screen with no back affordance and hand-rolled section headers; align with the `EmailNavBar` + `InsetSection` pattern of its five siblings, and give accent swatches press feedback + semantics.
- `lib/screens/onboarding/onboarding_screen.dart:571` — **fixed** — routine toggle hardcodes `c.move` while the icon tile uses `c.forType(colorKey)`.
- `onboarding_screen.dart:580` — deferred — bespoke `_Toggle` vs `CupertinoSwitch`; visual decision.
- `_SwitchRow`/`_StepperRow` re-implement `ListRow` geometry — deferred — needs a trailing-slot extension on `ListRow`; structural.

### Improvements
- `budgets_goals_screen.dart:52`, `export_data_screen.dart:44`, `onboarding_screen.dart:78` — **fixed** — async persistence/export calls have no try/catch; busy flags stick on failure. Wrap, reset in `finally`, surface errors.
- `notifications_screen.dart:84` — **fixed** — permission row shows an empty value before first request; show "Not set".
- `onboarding_screen.dart:16` — **fixed** — stale "pick-5 Rituals" doc comment (flow seeds 3 routines). `dailyRitualTarget: 5` vs 3 seeded routines — deferred, product decision.
- `about_screen.dart:17` — **fixed** — verified Profile does not actually display a version string (the audit misread those lines), so the constant only needed its stale doc comment corrected; no sharing required.
- `profile_screen.dart:226+` — deferred — hardcoded placeholder rows ("Apr 17–23", "3 new") and odd routings (Settings→budgetsGoals); prototype stubs needing product decisions.

## Data layer

### Errors
- `lib/data/seed/seeder.dart:107` — **fixed** — seed marker insert isn't idempotent like every other seed insert; use insert-or-replace and clean up old `initial_seed_*` markers.
- Mappers/migrations otherwise verified clean: enum wire formats consistent, schemaVersion 5 matches steps, seed FKs resolve.

### Inconsistencies
- Repo verb split (`save` / `setAssignment` / `upsert` for the same operation), `toModel()` vs `*FromRow` mapper conventions — **deferred** — renames ripple across features; do as one pass.

### Improvements
- Missing indexes on `entries.timestamp`, `entries.sourceRef`, and child FK columns — **deferred** — requires a schema migration; measure first per project convention.
- `lib/models/enums.dart:99` `Provider` enum possibly unused — **fixed** — verify references; remove only if zero.

## Cross-cutting deferred decisions

1. **Dead interactive controls** — Search (today), ellipsis menus (ask-pal, email dashboard, active session), Share (streak celebration, weekly review), "Tune what Pal notices", Swap, History & trends. One product decision: wire, hide, or de-emphasize. (The inbox footer's false button *semantics* are fixed now; the rest left visible.)
2. **White/scrim color tokens** — add `AppColors.onAccent`/scrim tokens and sweep ~15 files of raw literals.
3. **`CheckButton` adoption** — rituals `_StepRow` and workout set rows should use the shared control; changes visuals/haptics, do with design review.
4. **Streak semantics** — celebrated streak mixes ritual-seed data with workout framing and is never incremented; define the real source.
5. **Structural screen alignment** — Weekly Plan / Weekly Review / email nav onto `LargeTitleScrollView`/shared nav.
6. **Notification scheduling** — toggles must reconcile actual scheduled notifications.
7. **Composer undo** — pal composer auto-applies actions with no undo affordance.
8. **DB indexes** — schema v6 migration once query volume justifies it.

## Empty directories
- `lib/screens/money/` and `lib/services/health/` contain no files — remove or populate.
